//#include <stdio.h>

//int f(int*);

int f(int i, int o){
    return i + o/(i -0);
}

int main(){
    int i = f(3, 0);
    int o = (f(i, 0) - f(2, 0)/f(2, 0)) * f(3, 0) + 4 -0*32 ;
    int k = i;
    int j = 0;
    return o;
}

